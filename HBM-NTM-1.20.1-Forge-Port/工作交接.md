# 工作交接

## 当前目标

第二批核弹/基础爆炸物模型已完成资源与注册闭环。下一窗口应继续使用 `$hbm-ntm-migration`，优先做游戏内目检和核弹/爆炸物 BlockEntity 占位。

## 本轮已完成

- 建立 Forge 1.20.1 新工程骨架。
- 加入代理配置与一键启动客户端脚本。
- 处理 `processResources` 重复资源报错。
- 注册基础物品、基础机器、核弹创造栏。
- 迁移基础机器模型与材质。
- 修正高炉/机器朝向：摆放时正面朝向玩家。
- 迁移火力锻压机 OBJ 模型，并修正模型偏移与地面透明问题。
- 迁移首批核弹 OBJ 模型：`nuke_gadget`, `nuke_boy`, `nuke_man`, `nuke_tsar`。
- 迁移第二批核弹/基础爆炸物 OBJ 模型：`nuke_mike`, `nuke_prototype`, `nuke_fleija`, `nuke_solinium`, `nuke_n2`, `nuke_fstbmb`, `bomb_multi`。
- 修复核弹 MTL 一行写入导致的紫黑缺贴图问题。
- 将 Codex skill 从 TFC Addon 语境改造为 HBM NTM 迁移语境。
- 新增 HBM 迁移源码定位脚本和文档模板。

## 当前稳定结论

- 1.7.10 是主事实来源，1.20.1 参考版不能完全照搬。
- OBJ 模型迁移要重点检查原点、偏移、MTL 格式、贴图路径和 `noOcclusion`。
- 机器与核弹当前先做模型和注册，不做真实逻辑。
- 矿石生成和世界生成暂缓。

## 下一步建议

1. 进入游戏检查第二批模型贴图、朝向、遮挡、模型比例和偏移。
2. 若比例偏差明显，优先微调 `assets/hbm/models/block/*` 的 `display` 或 OBJ 顶点偏移。
3. 开始核弹/爆炸物 BlockEntity 占位，不迁移真实爆炸逻辑。
4. 继续补地雷/炸药类基础模型，例如 AP mine、naval mine、fat mine、charges。

## 验证记录

- `compileJava processResources --no-daemon` 曾通过。
- `processResources --no-daemon` 曾在修复核弹 MTL 后通过。
- `compileJava processResources --no-daemon` 已在第二批核弹模型迁移后通过。
- 使用正确转义的 `JAVA_OPTS` 代理命令执行 `processResources --no-daemon` 通过，代理为 `127.0.0.1:7890`。
- 游戏客户端可通过 `run-client.bat` 启动。

## 注意事项

- PowerShell 写多行 MTL 时不要把字符串数组意外拼成单行。
- 不要把 `nuke_custom` 作为下一步重点，先做第二批游戏内目检和基础爆炸物模型延伸。
- 如果 Gradle 需要下载依赖，使用代理 `127.0.0.1:7890`。
