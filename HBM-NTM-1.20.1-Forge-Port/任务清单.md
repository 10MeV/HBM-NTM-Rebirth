# 任务清单

## 进行中

### [TASK-20260518-03] 基础机器 BlockEntity/Menu/Screen

- 状态: 火力锻压机 UI/机制/输入物品动画/工作声音/真实 OBJ 压头动画已接入，后续扩展配方和更多机器动画
- 优先级: 中
- 依赖: 基础机器注册和模型稳定
- 验收条件: 至少一个基础机器可打开 GUI，可运行最小配方，并保留后续扩展入口
- 备注: `machine_press` 已按旧版源码迁移 13 槽 GUI 布局、燃料/模具/输入/输出/额外存储、速度加减速、左上角顺时针表盘指针、压头往复机制、`hbm:press` 配方入口、输入物品动画、压制完成工作声音、真实 OBJ 压头动画和破坏掉落内部物品；OBJ 动画库接口已留在 `client/obj`，包含旧版风格分件渲染接口、客户端平滑插值使用样板、block 坐标 OBJ 原点补偿和轻量客户端同步注意事项

### [TASK-20260518-02] 核弹/爆炸物 BlockEntity 占位

- 状态: 待处理
- 优先级: 中
- 依赖: 模型与注册稳定
- 验收条件: 方块具备未来逻辑入口，但不触发真实爆炸

## 已完成待归档

### [TASK-20260518-01] 补完剩余核弹和基础爆炸物模型

- 状态: 已完成资源与注册闭环，待游戏内目检模型比例
- 优先级: 高
- 来源文件: 1.7.10 `ModBlocks.java`, `ResourceManager.java`, `blocks/bomb`, `assets/hbm/models`, `assets/hbm/textures`
- 目标文件: 1.20.1 当前工程 `ModBlocks.java`, `HbmBlockStateProvider.java`, lang/resource/data 文件
- 参考文件: 1.20.1 参考移植版相关模型与资源
- 验收条件: 创造栏可见、可摆放、贴图不紫黑、朝向正确、无地面透明/遮挡问题、`compileJava processResources` 通过
- 备注: 已完成 `nuke_gadget`, `nuke_boy`, `nuke_man`, `nuke_tsar`, `nuke_mike`, `nuke_prototype`, `nuke_fleija`, `nuke_solinium`, `nuke_n2`, `nuke_fstbmb`, `bomb_multi`

## 待处理

### [TASK-20260518-04] 配方与数据生成体系扩展

- 状态: 机器配方注册骨架已开始，通用 DataGen/更多配方待处理
- 优先级: 中
- 依赖: 基础物品和机器 ID 稳定
- 验收条件: 支持基础 crafting/smelting/机器配方数据落地

### [TASK-20260518-05] 矿石与世界生成

- 状态: 后期统一处理
- 优先级: 低
- 依赖: 基础方块和资源体系稳定
- 验收条件: 不在当前阶段处理

## 待确认

### [TASK-20260518-06] 是否保留所有 1.7.10 核弹注册 ID

- 结论: 默认保留全部 1.7.10 核弹注册 ID
- 规则: 若个别 ID 与 1.20.1 命名或资源组织发生硬冲突，仅允许轻微现代化命名，且优先保持语义和可读性一致
- 状态: 已确认

### [TASK-20260518-07] 工程骨架与运行脚本

- 验收结果: 可进入游戏，`run-client.bat` / `run-client.ps1` 可用
- 状态: 已确认，已归档到 `历史修改.md`

### [TASK-20260518-08] 基础物品/机器/首批核弹模型

- 验收结果: 已完成首批注册、资源和游戏内测试修复
- 状态: 已确认，已归档到 `历史修改.md`

### [TASK-20260520-03] 全部已迁移 OBJ 模型物品栏中心校准

- 状态: 待处理
- 优先级: 高
- 目标: 所有已迁移的 OBJ block/item 模型在物品栏、掉落物和手持视图中都位于正中心，不再出现偏左、偏右、偏上或偏下的系统性偏移
- 依赖: 当前 OBJ 资源与 block 坐标补偿规则稳定
- 验收条件: 创造栏与物品栏中的全部 OBJ 模型视觉居中，且不影响世界方块坐标、BER/TESR 动画和碰撞形状

### [TASK-20260519-01] 辐射系统底层迁移

- 状态: 第一阶段已接入，待继续扩展
- 优先级: 高
- 依赖: 基础物品注册、事件总线、SavedData
- 验收条件: 物品辐射、玩家辐射、区块辐射、辐射效果、消辐效果和至少一个去污设备具备可测闭环
- 已完成: `radiation` 包、`CommonForgeEvents`、`ModEffects`、`geiger_counter`、`radaway/radaway_strong/radaway_flush`、`decon` 方块与 BlockEntity、当前核材料辐射值、`compileJava processResources` 验证
- 待处理: Hazmat/护甲防护批量注册、完整 HazardRegistry、辐射伤害类型、世界辐射破坏、辐射雾粒子、玩家数据网络同步、更多去污/抗辐设备

### [TASK-20260519-02] 辐射防护、危险属性与同步

- 状态: 第二阶段已接入，待游戏内验证和继续扩展条目
- 优先级: 高
- 已完成: `HazmatRegistry`/`ArmorUtil` 防护语义、`HazardRegistry` 注册接口、已迁移核材料/核弹危险属性、区块辐射世界副作用、辐射雾临时粒子、玩家辐射网络同步包和客户端缓存
- 待处理: HBM 护甲本体迁移后补全 Hazmat 条目；燃料棒/核废料/反应堆材料迁移后补全 HazardRegistry；绘制 HUD/剂量计；添加现代 `damage_type`；将世界副作用从原版泥土/空气替换成旧版 `waste_earth`/`waste_leaves`
- 验证: `compileJava processResources --no-daemon` 通过

### [TASK-20260519-03] 盖格计数器 HUD、危险提示与玩家辐射效果

- 状态: 已接入，待游戏内验收
- 优先级: 高
- 已完成: 背包盖格计数器点击声、右键 TechBoop、右下角玩家辐射 HUD、物品单个/整组辐射 tooltip、装备抗性 tooltip、RadAway 原版音效、玩家累积辐射分层效果、`HazardRegistry` 后续扩展入口
- 待处理: 使用真实游戏场景确认 HUD 位置是否遮挡其他模组 UI；迁移 HBM 防化服/装甲后补全 `HazmatRegistry`；迁移更多燃料棒、废料、滤罐和特殊物品后补全 `HazardRegistry` 条目；后续补现代 `damage_type` 和更完整的 1.7.10 辐射视觉效果
- 验证: `compileJava processResources --no-daemon` 通过

### [TASK-20260519-04] Digamma 辐射与旧版玩家辐射效果

- 状态: 第一阶段已接入，待游戏内验收与后续扩展
- 优先级: 高
- 已完成: `digamma_diagnostic` 物品/贴图/模型/语言键/右键诊断输出；`hfr_digamma` 数据；Digamma 对最大生命值的旧版 `0.5^digamma` 影响；10 DRX 致死语义；玩家普通辐射 200/400/600/800/1000 阈值随机效果；盖格 HUD 使用 1.7.10 `overlay_misc.png` 原始素材；新增 consumables 创造栏
- 待处理: 迁移真实 Digamma 污染来源（Digamma flame/matter/entity/武器）、Fau/robes/stability 等 Digamma 防护语义、旧版 vomit/sweat/radiation 客户端粒子网络和专用 damage_type
- 验证: `compileJava processResources --no-daemon` 通过

### [TASK-20260519-05] 实体通用辐射与放射性测试方块
- 状态: 已接入，待游戏内验收。
- 优先级: 高。
- 已完成: 所有 living entity 区块辐射污染、旧版分层辐射效果、满辐射/Digamma 自定义伤害死亡、低/高辐射呕吐与汗液粒子占位、Digamma 灵魂沙粒子、消辐宁/辐射 buff 图标裁切、`waste_earth`/`waste_mycelium` 测试方块与资源。
- 待处理: 完整迁移 1.7.10 AuxParticle 客户端粒子渲染；继续补核废料/燃料棒/辐射液体等真实辐射源；补更多实体变异目标（核苦力怕、鸭子/Quackos 等需要对应实体先迁移）。
- 验证: `compileJava processResources --no-daemon` 通过。

### [TASK-20260519-06] 普通物品大规模迁移
- 状态: 第一批已完成，待游戏内验收。
- 优先级: 高。
- 已完成: 157 个 `partsTab` plain `new Item()` 普通物品注册、HBM Parts 分类、1.7.10 原贴图迁移、基础 item model、语言 fallback、核素/燃料/废料 Hazard 接入。
- 待处理: 第二批迁移 `controlTab`/模板/电路/机器组件中仍是普通物品的部分；单独处理带行为类和旧 tooltip 的物品；枪械、弹药、武器链路最后迁移。
- 验证: `compileJava processResources --no-daemon` 通过。

### [TASK-20260519-07] 普通方块资源大规模迁移
- 状态: 第一批已完成，待游戏内验收。
- 优先级: 高。
- 已完成: 220 个 `blockTab` 普通材料/矿石/压缩块/建筑装饰方块注册、HBM Blocks 分类、1.7.10 原贴图复制、基础 blockstate/model/item model/loot/lang、放射性方块物品 Hazard 接入。
- 待处理: 带状态/发光/透明/复杂碰撞/特殊 TileEntity 或世界生成语义的方块；缺失贴图条目需继续回查资源包或延后；矿石真实掉落与世界生成另行迁移。
- 验证: `compileJava processResources --no-daemon` 通过。
### [TASK-20260519-08] 复杂碰撞方块与 OBJ 模型回查
- 状态: 第一批修复已完成，待游戏内验收。
- 优先级: 高。
- 已完成: 上一批普通方块中明显需要非整块碰撞/OBJ 的 `lantern`、`floodlight`、`spotlight_*`、`bobblehead`、`snowglobe`、`plushie`、`rebar`、`wood_barrier`、`sandbags` 已脱离普通 cube 形态；对应 1.7.10 OBJ/贴图已迁移并挂载。
- 待处理: `bobblehead`/`snowglobe`/`plushie` 的多变体 TileEntity、GUI、metadata 掉落和完整渲染；`lantern` 的旧版多格 dummy 结构；spotlight/floodlight 的朝向、红石/光束/能量逻辑。
- 验证: `compileJava processResources --no-daemon` 通过。

### [TASK-20260519-09] bobblehead/snowglobe/plushie 多变体 TileEntity 与 renderer
- 状态: 第一阶段已完成，待游戏内验收和特殊姿态精修。
- 优先级: 高。
- 已完成: `TrinketBlockEntity`、`TrinketBlock`、`TrinketBlockItem`、创造栏变体展开、NBT `Variant` 承接旧 metadata、放置/破坏/中键拾取保持变体、BER 渲染、snowglobe part 拆分、plushie 挤压计时和原素材迁移。
- 待处理: bobblehead 旧版特殊姿态和附属部件逐项迁移；Number Nine plushie 的旧版马模型/护甲部件渲染；bobblehead/snowglobe 旧版右键 GUI 如后续需要再迁移。
- 验证: `compileJava processResources --no-daemon` 通过。

### [TASK-20260520-01] OBJ 材质链路修复
- 状态: 已完成，待游戏内复验。
- 优先级: 高。
- 已完成: 为 trinket、spotlight、floodlight、lantern 相关 OBJ 补齐同名 `.mtl`，统一 `usemtl default` 与 `map_Kd #default`；本地扫描确认 OBJ/MTL 材质引用闭环；`compileJava processResources` 通过。
- 待处理: 进入游戏检查创造栏和放置后的模型是否仍有紫黑；如还有，需要查看 `latest.log` 中具体 ModelBakery/OBJLoader 报错路径。

### [TASK-20260520-02] 复杂 OBJ block 坐标与物品栏显示校准
- 状态: 第一轮已完成，待游戏内复验。
- 优先级: 高。
- 已完成: 扫描 spotlight/floodlight/lantern 顶点边界；生成 `_block.obj` 静态坐标副本；将 `spotlight_*`、`floodlight`、`lantern` 模型切换到 block 坐标副本；调整物品栏/掉落/手持 display 缩放和平移。
- 待处理: 游戏内复验具体剩余偏移；后续迁移 spotlight 朝向/光束、floodlight TileEntity 旋转/发光层、lantern 发光层时再用 BER 替代静态占位。
- 验证: `compileJava processResources --no-daemon` 通过。
